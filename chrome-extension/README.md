# Chrome Extension

Loads unpacked in Chrome and mirrors the current ChatGPT/OpenAI cookies into the local desktop helper.

## Install

1. Open `chrome://extensions`
2. Enable `Developer mode`
3. Click `Load unpacked`
4. Select the `chrome-extension` folder from this repo

The extension expects the helper at `http://127.0.0.1:48713` by default.
